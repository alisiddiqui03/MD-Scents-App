import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  // Core brand palette
  static const Color primary = Color(0xFF1F2A44); // Deep Blue
  static const Color secondary = Color(0xFF8B5CF6); // Soft Purple
  static const Color accent = Color(0xFFF59E0B); // Gold Accent

  // Backgrounds
  static const Color background = Color(0xFFF8FAFC); // Light Grey
  static const Color darkBackground = Color(0xFF0F172A);

  // Status
  static const Color success = Color(0xFF10B981);
  static const Color danger = Color(0xFFEF4444);

  // Text
  static const Color textDark = Color(0xFF111827);
  static const Color textLight = Color(0xFFFFFFFF);
}

